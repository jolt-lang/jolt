# host/scheme-adapter/CONTRACT.txt — the portable-scheme-layer CONTRACT.
#
# Every identifier here is something EVERY target's adapter must provide.
# On Chez these are all natives, so host/scheme-adapter/chez.ss asserts they
# are bound and defines NOTHING (zero wrappers, zero runtime cost). The
# portability gate allows contract names everywhere with NO per-file allowlist
# lines; the census still counts them under "contract:<tier>" so the
# inventory stays honest. This file is the source of truth for
# TARGET-CONTRACT.md (R10).
#
# Format mirrors portability-blocklist.txt: one identifier per line, tiers are
# "# tier: <name>" headers. The HOST CALL SHAPE is the binding shape — a
# target adapter must match it, not merely provide the name.
#
# Curated in R2: each entry must be something a portable implementation
# (Guile/WASM is the motivating target) can reasonably provide. Anything that
# failed that bar stayed on the blocklist (ffi, eval/compile, fasl,
# introspection, GC hooks, Chez-only list/vector odds) — those get routed
# through adapter capability entry points in R4-R9.
#
# tier: threads
# SRFI-18 basis (threads, mutexes, conditions). make-thread-parameter follows
# the SRFI-39 parameter discipline that R6RS parameterize/make-parameter
# establish. get-thread-id returns the current thread object.
#
# GET-THREAD-ID IS A NUMBER, DISTINCT PER LIVE THREAD (PSL R4, pinned): Chez
# returns a fixnum id. Host code multiplies it into the random seed
# (rt.ss:967) and keys an eqv?-hashtable by it (lazy-bridge.ss live-threads);
# no site assumes ordering. A target whose native thread ids are objects must
# map them to a numeric per-thread id for this contract name.
#
# CONDITION-WAIT MAY WAKE SPURIOUSLY (PSL R4, pinned): every host wait site
# re-checks its predicate in a loop (each site recorded in the R4 table);
# targets need not suppress spurious wakeups.
#
# THREAD-PARAMETER FORK-INHERITANCE (PSL R4, pinned): a thread created with
# fork-thread starts with the CREATING thread's CURRENT values of every
# thread parameter (the SRFI-18 semantic, and Chez's). The host depends on
# this in two directions and a target adapter must reproduce it:
#   - dyn-binding-stack (dyn-binding.ss): binding conveyance into futures,
#     agents, go blocks, and async callbacks. future/go/Thread.start/
#     executor forks ALSO re-install an explicit snapshot, but the
#     agent-worker forks (concurrency.ss:305, 467), the tap delivery thread
#     (concurrency.ss:530), and the async put!/take! callback forks
#     (async.ss:480-482, 490, 492) rely on inheritance ALONE — fresh-default
#     fork semantics would silently drop the creating thread's dynamic
#     bindings there.
#   - *txn* (refs.ss): the STM transaction record. Twelve fork bodies clear
#     it with an explicit (*txn* #f) because a child would otherwise inherit
#     the parent's in-flight transaction; the clears are written under (and
#     document) the inheritance model. R4 added the executor-worker clear
#     (concurrency.ss:902): a pool created inside a dosync forked workers
#     holding the stale txn record, so a job calling ref-set outside a txn
#     silently wrote into the dead log (never committed) instead of throwing
#     IllegalStateException like the JVM.
# A parameter used with the OPPOSITE assumption (fresh default per thread,
# worked around the inheritance — thread-interrupt-cell in
# host-static-methods.ss, random-seeded? in rt.ss, *agent-nested* in
# concurrency.ss) defends itself with an owner-identity check or an explicit
# reset at use, so those work under either model; they are noted per file in
# the R4 table, not re-pinned here.
#
# java.lang.ThreadLocal (host-static-classes.ss) used to be the one opposite-
# direction parameter that did NOT defend: under fork-inheritance a child
# inherited the parent's SET value instead of running its own initialValue. That
# was recorded here as an accepted divergence and it was not one — it is the only
# thing the class promises, and it silently gave every worker in a pool one
# shared ThreadLocal<Process> / RNG / SimpleDateFormat (jolt-uecg). It no longer
# uses a thread parameter: the value lives in a per-thread virtual-register
# table, so "fresh per thread" holds under either fork model.
# java.lang.InheritableThreadLocal, whose contract IS inheritance, keeps a
# per-instance thread parameter and so joins the load-bearing list above — an
# adapter whose threads do not inherit must arrange it explicitly there too.
#
# SA-THREAD-ID-OF answers a thread OBJECT's numeric id (the value get-thread-id
# returns inside it) from OUTSIDE it, or #f once the thread has exited. The
# fork-thread wrapper (lazy-bridge.ss) records the child under it in the
# forking thread, before the child has run a single safe point: a record the
# child makes of itself can come too late (a child started while a collection
# is pending traps at its entry and waits there first), and the stall report
# (rt.ss jolt-report-gc-stall) tells a thread jolt started from one it did
# not by this record. A target that cannot read a thread's id from its object
# answers #f and records nothing; every thread then reads as not started by
# jolt, which is why the Gambit host, answering #f, installs no stall watch.
#
# MUTEXES ARE NON-RECURSIVE (PSL R4, pinned): same-thread re-acquire blocks,
# per SRFI-18 mutex-acquire!. No host site re-acquires: re-entrant entry is
# handled structurally — nested dosync joins the outer transaction through
# the *txn* thread parameter (jolt-sync, refs.ss:159-161) and atom/ref/user
# callbacks are never invoked under a held lock (atoms.ss swap! runs f and
# watches outside the CAS lock; refs.ss fires watches after commit, outside
# stm-lock). A target whose mutexes are recursive must still satisfy every
# contract name with the non-recursive discipline, since host code relies on
# re-entrance NOT being recursion.
fork-thread
make-mutex
mutex-acquire
mutex-release
with-mutex
make-condition
condition-wait
condition-signal
condition-broadcast
make-thread-parameter
get-thread-id
sa-thread-id-of
# Fences. seq.ss publishes a forced tail behind memory-order-release so the
# seq's own fields are visible before the word that points to it on a weakly
# ordered machine; a target whose threads share no memory (green threads on
# one OS thread) provides both as no-ops.
memory-order-acquire
memory-order-release
#
# tier: system
# getenv: R7RS get-environment-variable shape — (getenv "NAME") -> string or #f.
# system: POSIX — runs the command via the shell and returns the exit status as
# an exact integer (build.ss checks (zero? rc)).
# sleep/current-time are R6RS (rnrs time) members, so the contract shape is the
# R6RS one: sleep takes a time object (the host passes (make-time ...) results),
# current-time takes an explicit time-type ('time-utc/'time-monotonic). The host
# ALSO calls (current-time) with zero args in run-corpus.ss — a Chez extension
# returning a time-utc object; Guile's SRFI-19 (current-time) matches that shape,
# so adapters must accept both the R6RS one-arg form and the zero-arg form.
getenv
sleep
current-time
system
#
# tier: capability-system
# OUR OWN definitions in host/chez/scheme-adapter-runtime.ss (loaded FIRST in
# the runtime manifest), not (chezscheme) exports — a target's adapter must
# provide these SAME call shapes. Each is a one-liner over a blocklisted
# system-tier native (open-process-ports / collect / bytes-allocated /
# memory-bytes / real-time); adaptercheck loads the runtime file and probes
# the combined environment for them. Permitted degradation, per entry point:
# sa-gc-collect may no-op (the JVM gc it maps is only a hint); sa-run-process
# must raise 'unsupported on targets without subprocess support (no caller
# tolerates a fake); sa-environment-pairs answers the whole environment as the
# R7RS get-environment-variables alist and never by running a program (it is
# what (System/getenv) and a ProcessBuilder child's inherited environment are
# built from); sa-os-release answers the OS's own version string or #f, and #f
# leaves os.version out of the system properties; sa-real-time-ms may use any monotonic ms clock but never
# a constant (callers stamp temp files with it); sa-total-memory-bytes may
# approximate — callers only display it or subtract sa-bytes-allocated from
# it; sa-max-memory-bytes is the heap's high-water mark since the last
# sa-reset-max-memory-bytes! and may answer the current total (a target that
# keeps no peak answers "now"), in which case sa-reset-max-memory-bytes! may
# no-op; sa-gc-reserve-ratio! may be ignored by a target that sizes its own
# reserve, and sa-gc-tight! by one without an in-place collection mode; sa-gc-trip-bytes may answer 0 where no trip threshold exists (the
# apply-scaling gate floors a footprint reading at it, and 0 is no floor);
# sa-gc-install-after-collect! may answer
# #f without installing anything, which leaves the heap unbounded as it was
# before 0.8.5 and the nursery fixed — a target with no collection hook cannot enforce a ceiling,
# and the caller then reports maxMemory as unbounded rather than promising a
# bound it does not have. sa-gc-install-stall-watch! may likewise answer #f:
# a target whose collector is not a rendezvous of active threads, or a Chez
# whose rendezvous protocol the Chez copy has not been compared against,
# keeps its own rendezvous and reports no stall (the behaviour before 0.8.9).
sa-run-process
sa-environment-pairs
sa-os-release
sa-gc-collect
sa-gc-max-generation
sa-bytes-allocated
sa-total-memory-bytes
sa-max-memory-bytes
sa-real-time-ms
sa-file-mtime-ms
sa-gc-trip-bytes!
sa-gc-trip-bytes
sa-gc-reserve-ratio!
sa-gc-tight!
sa-reset-max-memory-bytes!
sa-gc-install-after-collect!
sa-gc-install-stall-watch!
#
# tier: capability-introspect
# OUR definitions in host/chez/scheme-adapter-runtime.ss for the blocklist's
# introspect tier (machine-type / inspect/object / statistics). The machine
# tag is exposed ONLY as sa-host-tag for naming (release dirs, image headers,
# telemetry); every logic branch consumes the DERIVED properties
# (sa-os-family / sa-arch / sa-endian) — a Guile target has no ta6osx to give
# but can answer "what OS / arch / byte order is this". sa-stats degrades to
# a zero vector; the walker/procedure entry points degrade to '() / #f and
# the backtrace renders without frames, gated by sa-introspect-enabled?
# (a dynamic parameter — the degraded-backtrace gate flips it to prove throws
# still carry type+message). sa-procedure-code-name is the one entry the
# runtime reads for IDENTITY rather than for a report: (sa-procedure-code-name
# p) -> the string the procedure's code object was bound under, the same for
# every closure of one lambda, or #f; continuations.ss recognises an escape
# wrapper by it. A target that answers #f there makes escape-fn? answer false,
# which the contract permits; it must never answer a name for a procedure that
# was not bound under it.
sa-host-tag
sa-os-family
sa-arch
sa-endian
sa-stats
sa-introspect-enabled?
sa-continuation-frames
sa-procedure-info
sa-procedure-code-name
#
# tier: capability-ffi
# OUR OWN definitions in host/chez/scheme-adapter-runtime.ss (loaded FIRST in
# the runtime manifest), not (chezscheme) exports — a target's adapter must
# provide these SAME call shapes. Each is a one-liner over a blocklisted
# ffi-tier native (foreign-alloc/free/ref/set!/sizeof, lock-object/unlock-object,
# load-shared-object, foreign-entry?/foreign-callable-entry-point, or the
# Windows-path eval'd foreign-procedure construction); adaptercheck loads the
# runtime file and probes the combined environment. sa-foreign-procedure and
# sa-foreign-procedure-blocking are SYNTAX (compile-time-typed lowering to the
# native foreign-procedure form, __collect_safe in the blocking case) — a target
# must provide them as macros with the same lowering. Permitted degradation, per
# entry point:
# - sa-lock-object/sa-unlock-object may BOTH no-op on a non-moving collector
#   (never just one — that leaks or crashes);
# - sa-load-shared-object must raise on targets without dlopen, and
#   jolt.ffi/load-library must surface it as a clean jolt-level error;
# - sa-foreign-procedure-blocking may collapse to plain sa-foreign-procedure
#   ONLY if the collector never stops other threads (the R4 collect-safety
#   semantic);
# - the data-plane entries (alloc/free/ref/set!/sizeof) degrade none — a target
#   with the ffi capability provides them; without it, jolt.ffi raises cleanly;
# - sa-foreign-bytes-ref!/-set! are the BULK octet moves (a block copy between
#   foreign memory and a bytevector). A target with no block move may implement
#   them as a loop over its own foreign-ref/-set! — correct, and the constant
#   they exist to remove comes back. They pass the bytevector to C by address,
#   so a target must keep it address-stable for the call's duration and must
#   NOT make them collect-safe.
sa-foreign-alloc
sa-foreign-free
sa-foreign-ref
sa-foreign-set!
sa-foreign-bytes-ref!
sa-foreign-bytes-set!
sa-foreign-sizeof
sa-lock-object
sa-unlock-object
sa-load-shared-object
sa-foreign-entry?
sa-foreign-entry-address
sa-foreign-callable-entry-point
sa-foreign-procedure-runtime
sa-foreign-procedure
sa-foreign-procedure-native-error
sa-foreign-procedure-blocking
#
# tier: capability-continuations
# OUR OWN definition in host/chez/scheme-adapter-runtime.ss — the one-shot
# ESCAPE continuation, exposed to jolt programs as jolt.continuations
# (call-cc / letcc) via host/chez/continuations.ss.
#   (sa-call-with-escape-continuation proc) -> value: call PROC with a one-shot
#     escape procedure k; (k v) returns v from THIS call, unwinding the
#     dynamic-wind chain in between (so a jolt `finally` runs — an escape is a
#     real exit, unlike a fiber park, which drops those winders first).
# ENFORCED, NOT MERELY OFFERED: k is valid AT MOST ONCE and only while this
# call is still on the stack. A second invocation, or one after PROC returned
# normally, must RAISE — never re-enter a dead frame and never hang. A target
# whose only continuation primitive is multi-shot (Gambit's call/cc) carries a
# spent flag in its adapter to meet this; Chez's call/1cc is the contract
# natively, so its adapter is a one-liner.
# The layer above adds jolt policy the contract does not: which thread and
# fiber may invoke a given escape, and the jolt-level error. Targets do not
# implement that — they only owe the one-shot primitive.
# Permitted degradation: a target with no usable continuations must raise a
# message-carrying condition (never leave the name unbound); jolt.continuations
# then fails honestly on that target, the same shape jolt.image takes when
# fasl serialization is absent.
sa-call-with-escape-continuation
#
# tier: eval
# interaction-environment: R7RS (scheme repl), not Chez-only. The ONLY host
# call shape is the standard (eval expr (interaction-environment)) — jolt's
# compile spine evals emitted Scheme in the top-level environment (rt.ss's
# runtime procedures live there), so jolt's top level IS the interaction
# environment (def-var! targets it). A target adapter must provide an
# interaction environment whose eval sees the target's top level.
interaction-environment
#
# tier: capability-native-compile
# OUR OWN definitions in host/chez/scheme-adapter-runtime.ss — the eval/compile
# tier's compile-file / make-boot-file / compile parameters behind one entry
# surface. sa-compile-file compiles a Scheme source file to a native object
# under a TARGET-NEUTRAL profile alist: ((optimize . 0..3) (inspector-info .
# bool) (source-info . bool) (compressed . bool)); #f means target defaults. A
# target maps the keys it has and ignores the rest. Permitted degradation:
# sa-compile-file and sa-make-boot-file must RAISE on targets that cannot
# compile to native code — the AOT cache (loader.ss) treats the raise as a
# cache disable and loads from source, and `jolt build` must surface it as a
# clean jolt-level error, not a Scheme backtrace.
sa-baked-global
sa-compile-file
sa-make-boot-file
#
# tier: capability-image
# OUR OWN definitions in host/chez/scheme-adapter-runtime.ss — the fasl/image
# tier's fasl-write/fasl-read behind one entry surface. Shapes mirror the real
# call sites in state-image.ss exactly: (sa-fasl-write obj port [externals-pred])
# and (sa-fasl-read port [who exts]). Permitted degradation: must RAISE on
# targets without fasl serialization — jolt.image's dump/restore entry points
# surface a clean unsupported jolt-level error (verified: they already guard
# the write/read and convert a raise into a jolt-level message).
sa-fasl-write
sa-fasl-read
#
# tier: capability-unchecked
# OUR OWN definitions in the adapter runtime file, SYNTAX. The unchecked
# fixnum / vector primitives for the runtime's hot loops: the linear scan of a
# small map's slot vector, a slot-vector copy. Contract: on VALID input —
# fixnum operands whose result is a fixnum, a vector index in range — each
# gives exactly the checked primitive's result. On invalid input the behaviour
# is undefined, so a call site carries its own proof: a loop bound read from
# the vector's own length or a used-count kept <= it, an index stepped from 0
# by a constant. Degradation: a target may expand every one of these to the
# checked primitive (gambit does) — the speed goes, nothing else. On Chez they
# lower to the #3% (optimize-level 3) primitives, which is what makes a scan
# loop 3x faster in the optimize-level 2 runtime.
sa-ufx+
sa-ufx-
sa-ufx<?
sa-ufx>=?
sa-ufx=?
sa-uvector-ref
sa-uvector-set!
#   (sa-vector-copy-range! to at from start end) -> unspecified: copy FROM's
#     elements start..end-1 into TO at index at, the R7RS vector-copy! shape.
#     Chez's vector-copy! takes (from from-start to to-start count), so its
#     adapter reorders; a target with the R7RS order passes through. A bulk
#     move in the target's runtime — what beats a Scheme copy loop.
sa-vector-copy-range!
#   (sa-string-copy-range! to at from start end) -> unspecified: the same shape
#     over strings. Chez's string-copy! is (from from-start to to-start count)
#     while R7RS (Gambit, Guile) is (to at from start end) — the two orders
#     have the same arity, so a raw string-copy! in shared code compiles on
#     both targets and on the R7RS one silently copies the empty destination
#     INTO the source (the first block-copy subs did exactly that on gsi).
#     string-copy! is blocklisted for that reason.
sa-string-copy-range!
#
# tier: misc
# format is the Chez/Guile ~a ~s ~d subset (SRFI-28); printf/fprintf are
# format to the current/given output port. pretty-print is the standard
# pretty printer. box/unbox/set-box! are SRFI-111. gensym is R7RS. sort is the
# Chez argument shape (sort predicate list) — SRFI-132 has the opposite order,
# so adapters wrap. iota/list-head are SRFI-1 (take). vector-copy is SRFI-43.
# hashtable-values returns a vector of the table's values, hashtable-cells a
# list of (key . value) pairs (adapters build both from the R6RS
# hashtable-keys/for-each API). scheme-version (added R10): zero-arg, a
# human-readable version string of the target Scheme implementation — the
# exact format is target-specific (Chez's native returns "Chez Scheme Version
# 10.4.1"; a Guile adapter would wrap its (version)). Naming/telemetry only:
# no host logic may parse it except target-owned build machinery (release
# dirs, image headers), which knows its own target's format.
gensym
format
printf
fprintf
pretty-print
void
box
unbox
set-box!
sort
iota
list-head
vector-copy
hashtable-values
hashtable-cells
scheme-version
#
# tier: coroutines
# Stackful green threads sharing one OS thread (the carrier) — the fibers
# primitive, R1 of epic jolt-nvpr.2. The surface a scheduler drives; per R0's
# findings the Chez implementation keeps the per-fiber slice in ONE virtual
# register holding the current fiber record (a thread-parameter write is 33ns
# vs 2ns for a vreg — three writes per switch measured 8.6x and would drop the
# 3.4M switches/sec design point to ~350k) and the run queue is INTRUSIVE (the
# fiber record carries its next link — free by construction).
# Shapes:
#   (sa-fiber-spawn thunk) -> fiber: create a fiber running THUNK and make it
#     runnable; returns the fiber record. Spawning inside a fiber is legal.
#   (sa-fiber-yield) -> void: park the CURRENT fiber and move it to the back
#     of the run queue (round-robin); returns when the scheduler resumes it.
#     An error outside a fiber.
#   (sa-fiber-resume fiber) -> void: make a PARKED fiber runnable again. A
#     no-op when the fiber is already runnable — a double wakeup (the R4 alts!
#     commit race) must not corrupt the queue.
#   (sa-fiber-run-all) -> void: run the carrier's run queue until it drains —
#     the scheduler shape the plan names ("run ready fibers until the queue
#     drains, then block in the poller"; the poll step is R8). A fiber parked
#     via the resume path stops the drain until resumed. Exceptions inside a
#     fiber kill that fiber and never reach the sa-fiber-run-all caller —
#     per-fiber isolation.
# Permitted degradation: a target with NO usable continuations must still bind
# every name — to a message-carrying raise (never leave a name unbound); the
# plan's portability fallback is that `go` then runs on an OS thread, which is
# today's behavior. Gambit (R0(e)): call/cc-based implementation — the switch
# is ~10x the Chez cost interpreted and transparent IO parking does not port,
# so the target keeps `go` on OS threads (the plan's documented degradation).
sa-fiber-spawn
sa-fiber-yield
sa-fiber-resume
sa-fiber-run-all
